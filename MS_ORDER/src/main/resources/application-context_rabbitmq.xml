<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xmlns:context="http://www.springframework.org/schema/context"
     xmlns:util="http://www.springframework.org/schema/util"
     xmlns:aop="http://www.springframework.org/schema/aop"
     xmlns:tx="http://www.springframework.org/schema/tx"
     xmlns:rabbit="http://www.springframework.org/schema/rabbit"
     xmlns:p="http://www.springframework.org/schema/p"
     xsi:schemaLocation="
     http://www.springframework.org/schema/context
     http://www.springframework.org/schema/context/spring-context-3.0.xsd
     http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/util
     http://www.springframework.org/schema/util/spring-util-3.0.xsd
     http://www.springframework.org/schema/aop
     http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
     http://www.springframework.org/schema/tx
     http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
     http://www.springframework.org/schema/rabbit
     http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

    <context:component-scan base-package="com.service.impl.*"/>
    <context:property-placeholder location="classpath:application.properties"/>
    <!--连接配置-->
    <rabbit:connection-factory id="connectionFactory" host="${mq.host}"
                               username="${mq.username}" password="${mq.password}" port="${mq.port}"/>
    <rabbit:admin connection-factory="connectionFactory"/>

    <!--queque队里声明-->
    <!--durable 是否持久化
    exclusive 仅创建者可以使用的私有队里，断开后自动删除
    auto-delete 当所有消费者连接断开后，是否自动删除队列-->
    <rabbit:queue id="orderinfomation_queue" name="ms_orderinfomation" durable="true" exclusive="false" auto-delete="false"/>
    <rabbit:queue id="payinfomation_queue" name="ms_payinfomation" durable="true" exclusive="false" auto-delete="false"/>

    <!--交换机定义-->
    <!--交换机：一个交换机可以绑定多个队列，一个队列也可以绑定到多个交换机上面
    如果没有队列绑定到交换机上面，则发送到该交换机上的信息则会丢失

    direct模式：消息与一个特定的路由器完全匹配，才会转发
    topic模式：按模式匹配-->

    <rabbit:topic-exchange name="ms_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <!-- 设置消息Queue匹配的pattern (direct模式为key) -->
            <rabbit:binding queue="orderinfomation_queue" pattern="orderinfomation"/>
            <rabbit:binding queue="payinfomation_queue" pattern="payinfomation"/>
        </rabbit:bindings>
    </rabbit:topic-exchange>

    <bean name="orderinfoService" class="com.service.impl.OrderinfoService"></bean>
    <bean name="payinfoService" class="com.service.impl.PayinfoService"/>

    <!--配置监听消费者-->
    <rabbit:listener-container connection-factory="connectionFactory" acknowledge="auto">
        <rabbit:listener queues="orderinfomation_queue" ref="orderinfoService"/>
        <rabbit:listener queues="ms_payinfomation" ref="payinfoService"/>
    </rabbit:listener-container>
</beans>
